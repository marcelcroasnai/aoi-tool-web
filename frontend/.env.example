# ─── AOI Tool — frontend environment ──────────────────────────────────────────
# Copy to `.env`. Only needed if the frontend is served from a different host
# than the backend; otherwise the Vite dev proxy in vite.config.js handles /api.

VITE_API_URL=http://192.168.1.100:8000
